function stickyNavigation() {
    $("header").before($("header > .container").clone().removeClass("container").addClass("header -sticky").wrapInner(".container"))
}

function menuClose() {
    $(".-mobile-navigation").is(".-active") && ($(".-mobile-navigation").removeClass("-active"), $(".tcon-transform").removeClass("tcon-transform"))
}

function mobileNavigation() {
    $(".-mobile-navigation").toggleClass("-active")
}

function closeModal(e) {
    var a = $(e).closest(".modal"),
        o = a.find(".modal-content");
    a.removeClass("open"), o.empty()
}

function openModal(e) {
    var a = $(".modal");
    if (0 != $("body").find(a).length) {
        var o = a.find(".modal-content");
        if (a.addClass("open"), $(e).is(".photo") && 0 != $(e).find("img").length) {
            var t = $(e).find("img").attr("src");
            o.append("<div class='responsive-media' style='background:url(" + t + ") center center / cover no-repeat transparent'>")
        } else {
            var i = $(e).css("background-image").replace("url(", "").replace(")", "").replace(/\"/gi, "");
            o.append("<div class='responsive-media' style='background:url(" + i + ") center center / cover no-repeat transparent'>")
        }
    }
}

function tabs() {
    var e = $(".tabs"),
        a = e.find(".tabs-navigation a");
    a.each(function() {
        $(this).on("click", function() {
            var e = latinize($(this).attr("title").replace(/ /g, "-").toLowerCase()),
                a = $(this).closest(".tabs").find(".tabs-content");
            "tudo" != e ? (a.children().not("." + e).hide(), a.children("." + e).show()) : a.children().show()
        })
    })
}
var lastScrollTop = 0;
$(document).ready(function() {
    tabs(), 1140 > $(window).width() ? $(".header.-sticky").remove() : !$("#wrap>div.header").length && stickyNavigation(), $(window).on("resize", function() {
        1140 > $(window).width() ? $(".header.-sticky").remove() : !$("#wrap>div.header").length && stickyNavigation(), menuClose()
    }), $(window).scroll(function() {
        var e = $(this).scrollTop(),
            a = $(".navigation.-menu"),
            o = $(".header.-sticky");
        a.is(".-active") && e > lastScrollTop && $(window).height() > 414 && menuClose(), e > o.offset().top && o.addClass("-stuck"), 80 >= e && o.removeClass("-stuck"), lastScrollTop = e
    }), $(".webdoor .owl-carousel,.galeria .owl-carousel").owlCarousel({
        loop: !1,
        margin: 0,
        items: 1,
        itemsDesktop: !1,
        itemsDesktopSmall: !1,
        itemsTablet: !1,
        itemsMobile: !1,
        nav: !0,
        dots: !0,
        itemElement: "article",
        lazyLoad: !0,
        navText: ["<i class='fa fa-angle-double-left'></i>", "<i class='fa fa-angle-double-right'></i>"]
    }), $(".acomodacoes .owl-carousel").owlCarousel({
        loop: !1,
        margin: 25,
        nav: !0,
        itemElement: "article",
        lazyLoad: !0,
        navText: ["<i class='fa fa-angle-double-left'></i>", "<i class='fa fa-angle-double-right'></i>"],
        responsive: {
            0: {
                items: 1,
                dots: !1
            },
            600: {
                items: 2
            },
            1e3: {
                items: 3,
                dots: !0
            }
        }
    })
});